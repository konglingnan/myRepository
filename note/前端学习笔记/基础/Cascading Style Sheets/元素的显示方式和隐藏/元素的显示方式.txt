






		元素的显示方式：


			1.块级元素（block 独占一行，可设宽高。eg. div p h1 ul li dl）

			2.行内元素（inline 可多个标签同一行，大小由内容决定，不可设宽高。eg.  span buis一类）

			3. *行内块级元素（inline-block 可多个标签同一行，可设宽高。 eg. img）



		*元素显示方式的转换:

			修改display
