






			图片和文字显示在同一行时：

				1.默认情况下，文字的基线 会保持和 图片的底边 对齐。

				2.这个情况下，给图片设置margin-top，文字的基线 仍然会保持和 图片的底边 对齐。

			如果想让文字的中线和图片的中线保持对齐，要给图片设置样式：

				display: inline-block;
				vertical-align: middle;

				此时再给图片设置margin-top，文字的中线会和图片变成的盒子（但包括margin）中线保持对齐。
